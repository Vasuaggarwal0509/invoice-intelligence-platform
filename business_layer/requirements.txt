# Business layer runtime dependencies.
#
# Not separately installed — composed into the root requirements.txt via
# `-r business_layer/requirements.txt`. `pip install -r requirements.txt`
# at repo root picks these up.

# --- ORM + config --------------------------------------------------------
sqlalchemy>=2.0
pydantic-settings>=2.0
email-validator>=2.0         # enables pydantic EmailStr (CA signup DTOs)

# --- Auth / crypto -------------------------------------------------------
argon2-cffi>=23.1            # password hashing (argon2id)
cryptography>=42.0           # AES-GCM for OAuth token encryption
itsdangerous>=2.2            # HMAC-signed opaque tokens (OAuth state, CSRF)

# --- Content sniffing ----------------------------------------------------
python-magic>=0.4            # MIME-by-magic-bytes for upload validation

# --- Multipart uploads ---------------------------------------------------
python-multipart>=0.0.9      # required by FastAPI for UploadFile/Form

# --- Observability -------------------------------------------------------
structlog>=24.1              # structured JSON logs + PII redaction processor

# --- Email ingestion (Gmail connector) ----------------------------------
google-auth>=2.29            # credential models + refresh flow
google-auth-oauthlib>=1.2    # server-side OAuth 2.0 Flow (web client)
google-api-python-client>=2.130  # Gmail service discovery + HTTP transport
