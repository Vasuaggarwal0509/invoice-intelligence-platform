# Root requirements — runtime only. Composes per-layer deps.
#
# Production / container install:
#   pip install -r requirements.txt && pip install -e .
#
# Development install (adds pytest/ruff/mypy/bandit/pip-audit):
#   pip install -r requirements-dev.txt && pip install -e .
#
# The `-e .` step makes both `extraction_layer` and `business_layer`
# importable as editable packages in this venv (see pyproject.toml).

# --- Layer deps (composed) ------------------------------------------------
-r extraction_layer/requirements.txt
-r business_layer/requirements.txt
