# Requirements for the Invoice Intelligence Platform prototype.
#
# Install (PowerShell, from repo root):
#   uv venv venv
#   .\venv\Scripts\Activate.ps1
#   uv pip install -r requirements.txt
#
# Keep in sync with pyproject.toml [project.dependencies] and
# [project.optional-dependencies.dev].

# --- Runtime ---------------------------------------------------------------
rapidocr-onnxruntime>=1.3.0
pydantic>=2.0
numpy>=1.24
pillow>=10.1
datasets>=2.14
rapidfuzz>=3.0
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Dev / test ------------------------------------------------------------
pytest>=7.0
pytest-cov
